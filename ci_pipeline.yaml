kind: template
load: python_test.yaml

data:
  push_image : true
  test:
    vars: 
      PORT : 3000
  services:
    redis:
      active: false
    psql:
      active: false
      password: secret
      user: devops-awx-mcp
      database: devops-awx-mcp-db