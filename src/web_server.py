# Legacy module — kept for backwards compatibility
# The MCP server now runs directly via mcp.run(transport="streamable-http")
# Health checks are registered as custom_route on the MCP Starlette app.
